
import React from 'react';

export default function (props)
{
    return (
        <main className="main">
            {/* <Header /> */}
            {props.children}
            {/* <Footer /> */}
        </main>
    );
}
